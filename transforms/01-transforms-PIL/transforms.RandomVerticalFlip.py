'''

torchvision.transforms.RandomVerticalFlip(p=0.5)[SOURCE]
Vertically flip the given PIL Image randomly with a given probability.

Parameters
p (float) – probability of the image being flipped. Default value is 0.5

'''